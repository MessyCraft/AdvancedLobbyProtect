name: LobbyProtect
description: ${project.description}
version: '${project.version}'

main: io.github.messycraft.lobbyProtect.LobbyProtectPlugin
api-version: '1.13'

authors: [ ImCur_ ]

permissions:
  lobbyprotect.bypass:
    description: 绕过大厅常规保护（破坏/放置方块等）
    default: op
